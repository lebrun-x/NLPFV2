<!DOCTYPE html>

<html lang="fr">
	<head>
		<meta charset="UTF-8">
        
        <title> Tipz </title>

        <script src="{{ asset('./js/client/jquery.js') }}"></script>
        <script src="{{ asset('./js/client/utils.js') }}"></script>

        <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://code.jquery.com/jquery-1.11.2.min.js"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/js/bootstrap.min.js"></script>

        <link rel="stylesheet" href="{{ asset('./css/project_details.css') }}" />
    </head>


	<body>
        <nav class="navbar navbar-default">
            <ul class="nav nav-pills">
                  <li role="presentation"><a href="/home">Accueil</a></li>
                  <li role="presentation"><a href="/projet">Ajouter</a></li>
                  <li role="presentation"><a href="/best">Les meilleurs projets</a></li>
                  <li role="presentation"><a href="user.html.twig">S'enregistrer</a></li>
                  <li role="presentation"><a href="/connexion">Se connecter</a></li>
            </ul>
        </nav>
          <span id="project">
               <div class="panel panel-primary">
                <div class = "panel-heading"> {{ project.name }} </div>
                <div class = "panel-body">
                    <img id="imageproject" src= "{{project.image}}" alt="Image"/>
                    <div class="gain">
                        <span class="glyphicon glyphicon-star" aria-hidden="true"></span> Gain: {{project.amount}} € / mois
                    </div>
                    <div class="contact">
                        <span class="glyphicon glyphicon-user" aria-hidden="true"></span> Crée le: 12/01/2017<br/>
                    </div>
                    <div class="desctitle"><h4>Description</h4></div>
                    <div class="description"> {{project.description}} </div>
                </div>
            </div>

          </span>
          <span id="compensations">
              {% for compensation in compensations %}
              <div class="panel panel-primary" id="com">
                <div class = "panel-heading"> {{compensation.name}} - {{ compensation.amount }} €</div>
                    <div class = "panel-body">
                        <dl>
                          <dt>Description</dt>
                          <dd> {{ compensation.description }} </dd>
                        </dl>
                        <button class="btn btn-primary" onclick="window.location.href='/participate/{{ project.project_id }}/{{ compensation.compensation_id }}'">Participer</button>
                    </div>
                </div>
              {% endfor %}

          </span>
        </div>
    </body>
</html>